export enum ActionType {
    ECHO = "echo",
    DELAY = "delay",
    LOG = "log",
    ORIGIN = "origin",
    STATIC = "static",
    STATUS_CODE = "status_code",
    AUTHENTICATION = "authentication"
};